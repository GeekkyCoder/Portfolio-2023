export type GitHubGraphConfig = {
  username: string;
  /** Hex color without #, used by the chart image service. */
  accentHex: string;
};

export const githubGraphConfig: GitHubGraphConfig = {
  username: "GeekkyCoder",
  accentHex: "f44336",
};
